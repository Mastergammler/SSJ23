#include <iostream>
#include <windows.h>

/**
 * Entry point for working with the window api
 */
int WinMain(HINSTANCE hInstance,
            HINSTANCE hPrevInstance,
            LPSTR lpCmdLine,
            int nShowCmd)
{
    std::cout << "Hello window world" << std::endl;
}
